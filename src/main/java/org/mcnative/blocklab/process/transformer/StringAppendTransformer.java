package org.mcnative.blocklab.process.transformer;

import org.mcnative.blocklab.process.Context;
import org.mcnative.blocklab.process.Parameter;
import org.mcnative.blocklab.process.Transformer;

import java.util.Collection;

public class StringAppendTransformer implements Transformer {



    @Override
    public void transform(Context context) {
        if(context.getParameter("").getAs(Collection<String>)).for (:
                                                                    ) {

        }


        for (Parameter parameter : context.getParameters()) {
            if(parameter instanceof String) {
              //.append
            } else {
                //transformer
            }
        }
        return builder.build()
    }
}
